$(function(){
        $("#page").twbsPagination({

            totalPages:${moviePage.totalPageSize},
            visiblePages:10,
            first:'首页',
            pre:'上一页',
            next:'下一页',
            last:'尾页',
            href:'?p={{number}}'

        });
    });